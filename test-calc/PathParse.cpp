#include "stdafx.h"
#include "PathParse.h"

vector<string> * PathParse::Parse()
{
	if (mStrPath == "") return nullptr;
	vector<string> * vString = new vector<string>();
	[]() = > {};
	return vString;
}

PathParse::~PathParse()
{
}
